# PokeHub UI environment variables
# Copy to .env and update values

# Backend API base URL (used server-side in SvelteKit actions)
API_BASE_URL=http://localhost:3000

# Public origin for the UI (used in CORS / cookie SameSite configuration)
PUBLIC_ORIGIN=http://localhost:5173
